package com.mosis.stepby.utils.running

import org.osmdroid.util.GeoPoint

object TrackFilter {
   // suspend fun inRadius(center: GeoPoint, radius: Double): List<Track> {}
   // suspend fun withFlooring(type: TrackFlooring): List<Track> {}
}